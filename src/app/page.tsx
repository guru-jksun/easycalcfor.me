// This page is not used — the middleware redirects "/" to the
// appropriate locale (e.g. /en, /ko) based on the browser's
// Accept-Language header. This file exists only to satisfy Next.js.
export default function RootPage() {
  return null;
}
